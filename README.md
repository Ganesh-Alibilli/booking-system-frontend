# Frontend

Install dependencies and run:
```
cd frontend
npm install
npm run dev
```

Frontend expects backend API proxied at /api (if running both locally, you may use a proxy or run backend on port 4000 and use a dev proxy).
